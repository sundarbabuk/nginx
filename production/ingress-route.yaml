apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: nginx
  namespace: production
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`nginx.devsecops.com`)
      kind: Rule
      services:
        - name: nginx
          port: 80
